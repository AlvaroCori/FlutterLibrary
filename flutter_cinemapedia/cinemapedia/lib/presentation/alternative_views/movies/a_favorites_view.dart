import 'package:flutter/material.dart';

class AFavoritesView extends StatelessWidget {
  const AFavoritesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Favorite view alternative"),
      ),
      body: const Center(
        child: Text("ww"),
      ),
    );
  }
}